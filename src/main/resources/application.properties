spring.application.name=Tornet
spring.messages.basename=messages 
spring.messages.encoding=UTF-8

#Thymeleaf
spring.thymeleaf.cache=false

#Template
spring.thymeleaf.prefix=classpath:/templates/

#DATABASE
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.url=${JDBC_DATABASE_URL:mysql://localhost:3306/t123}

spring.jpa.show-sql=true
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=update
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.error.path=/error-page

#Logging
server.port=${PORT:8889}

spring.mvc.static-path-pattern=/static/**
spring.main.allow-circular-references=true
spring.mvc.view.prefix=/templates/
spring.mvc.view.suffix=.html

spring.mvc.hiddenmethod.filter.enabled=true

# JavaMailSender configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:taras.royale@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ltge zueq mqxf bazx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true




