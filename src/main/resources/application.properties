# Application name
spring.application.name=Tornet

# Thymeleaf settings
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/

# Database settings
# Use environment variable for the ClearDB URL if available, otherwise use default JAWSDB settings
spring.datasource.url=${CLEARDB_DATABASE_URL:jdbc:mysql://gk90usy5ik2otcvi.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/dcat7wvuv68n4nxc}
spring.datasource.username=${DB_USERNAME:clvv4cmymuz90gvw}
spring.datasource.password=${DB_PASSWORD:tk00rforcd28f3nr}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate settings
spring.jpa.show-sql=true
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server settings
server.port=${PORT:8889}
server.error.path=/error-page

# MVC settings
spring.mvc.static-path-pattern=/static/**
spring.mvc.view.prefix=/templates/
spring.mvc.view.suffix=.html
spring.mvc.hiddenmethod.filter.enabled=true

# JavaMailSender settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:taras.royale@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ltgezueqmqxfbazx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


